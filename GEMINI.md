# GEMINI.md

Gemini CLI and Antigravity: follow the instructions in [AGENTS.md](AGENTS.md). In short: `skills/` and `commands/` are the source of truth, generated files are rebuilt with `scripts/build_*.py`, and `python3 tests/validate_skills.py` must pass.
