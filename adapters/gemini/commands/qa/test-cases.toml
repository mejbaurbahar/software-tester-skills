# Generated from commands/test-cases.md by scripts/build_adapters.py - do not edit by hand.
description = "Design test cases with formal techniques (EP, BVA, decision tables, state, pairwise)"
prompt = '''
Design test cases for: {{args}}

Use the `test-design-techniques`, `manual-testing` and `functional-testing` skills.

1. Read the requirement or code named in {{args}}. If it is ambiguous, list the questions first (see `requirements-testing-review`).
2. Choose techniques from the shape of the input: partitions/boundaries, business-rule tables, state models, parameter combinations.
3. Show the model first (partitions, decision table, or state diagram), then the cases.
4. Output a table: ID, title, technique, priority, preconditions, steps, test data, expected result. Include negative and boundary cases.
5. Mark which cases should be automated and at which level.
'''
